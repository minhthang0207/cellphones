import VerifyForm from "@/components/organisms/Auth/VerifyForm";
import React, { Suspense } from "react";
const VerifyPage = () => {
  return (
    <Suspense>
      <VerifyForm />
    </Suspense>
  );
};

export default VerifyPage;
